<%= simple_form_for @board do |f| %>
  <%= f.input_field :board_id, value: @board.id, hidden: true %>
  <%= f.input :name %>
  <%= f.association :colors, collection: Color.all.sort, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>